{% include header.html %}

	<article class="entry">
				
		<header class="entry-header">

			{% if page.title %}<h1 class="entry-title">{{ page.title }}</h1>{% endif %}
			
			<p class="entry-meta secondary note">{{ page.date | date: "%b %-d, %Y" }}</p>

		</header>

		<div class="entry-content">
	
			{{ content }}
		
		</div>
		
		<footer class="entry-footer">
		
			{% capture words %}
				{{ page.content | number_of_words }}
			{% endcapture %}
			
			<ul class="nav secondary note">
			
				<li class="nav-item">{ words }} words</li> 			
				<li class="nav-item"><a href="/archive">Archive</a></li>
				<li class="nav-item"><a href="/feed">RSS</a></li>
			
			</ul>
			
			<div class="pagination clearfix">
			
				{% if page.next %}
			
					<p class="secondary next">Next: <a href="{{ page.next.url }}">{{ page.next.title }}</a> &rarr;</p>
			
				{% endif %}
			
				{% if page.previous %}
			
					<p class="secondary previous">&larr; Previous: <a href="{{ page.previous.url }}">{{ page.previous.title }}</a></p>
			
				{% endif %}
				
			</div>
			
		</footer>

	
	</article>
	
{% include footer.html %}
