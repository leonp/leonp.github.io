---
layout: default
title: Archive
description: Curated posts from 2008. A mix of web, literature, politics and a little music.
show: yes
---

<form class="pure-form form-custom" role="search">

	<input type="search" class="pure-input-2-3" id="search-query" placeholder="Search this site with Google">
	<input type="submit" class="pure-button pure-button-primary" value="Search">

</form>

{% for post in site.posts  %}

	{% capture this_year %}{{ post.date | date: "%Y" }}{% endcapture %}

	{% if forloop.first %}
	
		<h2>{{this_year}}</h2>

		<ul class="complex-list">
	
	{% else %}
	
		{% if this_year != last_year %}
	
		</ul>

		<h2>{{this_year}}</h2>

		<ul class="complex-list">
	
		{% endif %}

	{% endif %}

			<li>
	
				<div class="complex-list-title"><a class="link-list-link" href="{{ post.url | prepend: site.baseurl }}">{{ post.title }}</a></div>
		
				<div class="complex-list-info meta secondary"><time>{{ post.date | date: "%b %-d, %Y" }}</time></div>

			</li>

	{% if forloop.last %}

		</ul>
	
	{% endif %}

	{% capture last_year %}{{ this_year }}{% endcapture %}

{% endfor %}
